<UserControl x:Class="PhoneBookApp.Views.AddNewContactView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PhoneBookApp.Views"
        xmlns:converters="clr-namespace:PhoneBookApp.Converters"
        xmlns:enums="clr-namespace:PhoneBookApp.Models"
        xmlns:sys="clr-namespace:System;assembly=mscorlib"
        mc:Ignorable="d"
        d:DesignHeight="450" d:DesignWidth="800">

    <UserControl.Resources>
        <converters:TextToImageConverter x:Key="textToImageConverter"></converters:TextToImageConverter>
        <ObjectDataProvider x:Key="GenderEnumSource" ObjectType="{x:Type sys:Enum}" MethodName="GetValues">
            <ObjectDataProvider.MethodParameters>
                <x:Type TypeName="enums:Enums+Gender"/>
            </ObjectDataProvider.MethodParameters>
        </ObjectDataProvider>
    </UserControl.Resources>
    
    <Grid>
        <Grid>
            <StackPanel HorizontalAlignment="Center" >
                <Border BorderBrush="Aqua" BorderThickness="2" CornerRadius="50" Height="105" Width="105">
                    <Image Width="100" Height="100" Source="{Binding ElementName=ProfilePictureTB, Path=Text, Converter={StaticResource textToImageConverter}}" >
                        <Image.Clip>
                            <EllipseGeometry Center="50, 50" RadiusX="50" RadiusY="50"/>
                        </Image.Clip>
                    </Image>
                </Border>

                <Grid Margin="50 25 0 0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="auto"/>
                        <RowDefinition Height="auto"/>
                        <RowDefinition Height="auto"/>
                        <RowDefinition Height="auto"/>
                        <RowDefinition Height="auto"/>
                        <RowDefinition Height="auto"/>
                        <RowDefinition Height="auto"/>
                        <RowDefinition Height="auto"/>
                        <RowDefinition Height="auto"/>
                    </Grid.RowDefinitions>

                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="auto"/>
                        <ColumnDefinition Width="auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Text="First Name" Grid.Row="0" Grid.Column="0" Width="100" Margin="0 10 0 0"/>
                    <TextBox x:Name="FirstNameTB" Text="{Binding NewContact.FirstName, Mode=TwoWay}" Grid.Row="0" Grid.Column="1" Width="250" Margin="25 10 0 0" />

                    <TextBlock Text="Last Name" Grid.Row="1" Grid.Column="0" Width="100" Margin="0 10 0 0"/>
                    <TextBox x:Name="LastNameTB" Text="{Binding NewContact.LastName}" Grid.Row="1" Grid.Column="1" Width="250" Margin="25 10 0 0" />

                    <TextBlock Text="Phone Number" Grid.Row="2" Grid.Column="0" Width="100" Margin="0 10 0 0"/>
                    <TextBox x:Name="PhoneNumberTB" Text="{Binding NewContact.PhoneNumber}" Grid.Row="2" Grid.Column="1" Width="250" Margin="25 10 0 0" />

                    <TextBlock Text="Department" Grid.Row="3" Grid.Column="0" Width="100" Margin="0 10 0 0"/>
                    <TextBox x:Name="DepartmentTB" Text="{Binding NewContact.Department}" Grid.Row="3" Grid.Column="1" Width="250" Margin="25 10 0 0" />

                    <TextBlock Text="Email" Grid.Row="4" Grid.Column="0" Width="100" Margin="0 10 0 0"/>
                    <TextBox x:Name="EmailTB" Text="{Binding NewContact.Email}" Grid.Row="4" Grid.Column="1" Width="250" Margin="25 10 0 0" />

                    <TextBlock Text="Description" Grid.Row="5" Grid.Column="0" Width="100" Margin="0 10 0 0"/>
                    <TextBox x:Name="DescriptionTB" Text="{Binding NewContact.Description}" Grid.Row="5" Grid.Column="1" Width="250" Margin="25 10 0 0" />

                    <TextBlock Text="Gender" Grid.Row="6" Grid.Column="0" Width="100" Margin="0 10 0 0"/>
                    <ComboBox Name="GenderCB" ItemsSource="{Binding Source={StaticResource GenderEnumSource}}" SelectedItem="{Binding NewContact.Gender}" Grid.Row="6" Grid.Column="1" Width="250" Margin="25 10 0 0"/>

                    <TextBlock Text="Profile Picture" Grid.Row="7" Grid.Column="0" Width="100" Margin="0 10 0 0"/>
                    <StackPanel Grid.Row="7" Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right" >
                        <TextBox x:Name="ProfilePictureTB" Text="{Binding NewContact.PicturePath, Mode=TwoWay}" Width="175" Margin="5 10 0 0"/>
                        <Button Content="Choose" Width="70" Margin="5 10 0 0" Command="{Binding ShowFileDialogCommand}"/>
                    </StackPanel>

                </Grid>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0 25 0 0">
                    <Button Content="Add New Contact" Width="100" Margin="10 0 70 0" Command="{Binding AddNewContactCommand}" CommandParameter="{Binding NewContact}"/>
                    <Button Content="Return To List" Width="100" Margin="50 0 10 0" Command="{Binding ReturnToListCommand}"/>
                </StackPanel>
            </StackPanel>

        </Grid>
    </Grid>
</UserControl>
